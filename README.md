# Frontend (React + Vite + Tailwind)

## Setup
```bash
npm install
npm run dev
```

- Proxy to backend is configured in `vite.config.js`.
- Extend sections in `src/sections` as needed (About, Routes, FAQ, etc.).
